<x-app-layout><x-slot name="header"><h1 class="text-2xl font-extrabold">Darurat Masuk</h1><p class="text-sm text-slate-500">Sinyal SOS warga yang memerlukan tindak lanjut.</p></x-slot><div class="space-y-3">@forelse($daruratAktif as $item)<article class="rounded-2xl border border-red-200 bg-white p-5"><div class="flex flex-wrap items-start justify-between gap-3"><div><strong class="text-red-700">{{ $item->jenis_darurat }} · {{ $item->user->name }}</strong><p class="mb-1 text-sm">{{ $item->nomor_telepon }} · {{ $item->created_at->diffForHumans() }}</p><a target="_blank" href="https://maps.google.com/?q={{ $item->latitude }},{{ $item->longitude }}" class="font-bold text-blue-600">Buka Lokasi GPS</a></div><form method="POST" action="{{ route('admin.darurat.update',$item) }}" class="flex gap-2">@csrf @method('PATCH')<select name="status" class="rounded-lg border-slate-300 text-sm"><option value="aktif">Aktif</option><option value="ditangani">Ditangani</option><option value="selesai">Selesai</option></select><button class="rounded-lg bg-emerald-600 px-4 text-sm font-bold text-white">Update</button></form></div></article>@empty<p class="rounded-2xl bg-white p-8 text-center text-slate-500">Tidak ada kondisi darurat aktif.</p>@endforelse</div></x-app-layout>
