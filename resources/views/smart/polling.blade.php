<x-app-layout><x-slot name="header"><h1 class="text-2xl font-extrabold">Polling Desa</h1><p class="text-sm text-slate-500">Berikan suara untuk keputusan bersama.</p></x-slot><div class="grid gap-5 lg:grid-cols-2">@forelse($items as $poll)<form method="POST" action="{{ route('polling.vote',$poll) }}" class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">@csrf<h2 class="text-lg font-extrabold">{{ $poll->judul }}</h2><p class="text-sm text-slate-500">{{ $poll->deskripsi }}</p><div class="space-y-2">@foreach($poll->options as $option)<label class="flex cursor-pointer justify-between rounded-xl border border-slate-200 p-3"><span><input type="radio" name="polling_option_id" value="{{ $option->id }}" required> {{ $option->opsi }}</span><strong>{{ $option->votes->count() }} suara</strong></label>@endforeach</div><button class="mt-4 rounded-xl bg-emerald-600 px-5 py-2 font-bold text-white">Pilih</button></form>@empty<p>Belum ada polling aktif.</p>@endforelse</div></x-app-layout>
