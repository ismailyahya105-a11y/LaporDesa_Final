<x-app-layout>
<x-slot name="header"><h1 class="text-2xl font-extrabold">Pusat Informasi Desa</h1><p class="text-sm text-slate-500">Transparansi dan informasi resmi untuk warga.</p></x-slot>
<div class="grid gap-6 lg:grid-cols-2"><section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 class="font-extrabold">Transparansi APBDes {{ now()->year }}</h2><div class="h-72"><canvas id="apbdesChart"></canvas></div></section><section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 class="mb-4 font-extrabold">Pengumuman Digital</h2><div class="space-y-3">@forelse($pengumuman as $item)<article class="rounded-xl border border-slate-200 p-4"><span class="text-xs font-bold uppercase text-blue-600">{{ $item->jenis }}</span><h3 class="font-bold">{{ $item->judul }}</h3><p class="mb-0 text-sm text-slate-600">{{ $item->isi }}</p></article>@empty<p class="text-sm text-slate-500">Belum ada pengumuman.</p>@endforelse</div></section></div>
<section class="mt-6 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 class="mb-4 font-extrabold">Kontak Penting</h2><div class="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">@foreach($kontak as $item)<a href="tel:{{ $item->telepon }}" class="rounded-xl bg-slate-50 p-4 text-slate-800"><strong class="block">{{ $item->jabatan }}</strong><span class="text-sm">{{ $item->nama }}</span><small class="block text-emerald-600">{{ $item->telepon }}</small></a>@endforeach</div></section>
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.9/dist/chart.umd.min.js"></script><script>new Chart(document.getElementById('apbdesChart'),{type:'bar',data:{labels:@json($apbdes->pluck('kategori')),datasets:[{label:'Anggaran',data:@json($apbdes->pluck('anggaran')),backgroundColor:'#2563eb'},{label:'Realisasi',data:@json($apbdes->pluck('realisasi')),backgroundColor:'#16a34a'}]},options:{maintainAspectRatio:false}});</script>
</x-app-layout>
