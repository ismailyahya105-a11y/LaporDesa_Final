<x-app-layout><x-slot name="header"><h1 class="text-2xl font-extrabold">E-Musrenbang</h1><p class="text-sm text-slate-500">Sampaikan dan dukung ide pembangunan desa.</p></x-slot><form method="POST" action="{{ route('usulan.store') }}" class="mb-6 rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200">@csrf<div class="grid gap-3 md:grid-cols-[1fr_2fr_auto]"><input name="judul" placeholder="Judul usulan" class="rounded-xl border-slate-300" required><input name="isi" placeholder="Jelaskan usulan pembangunan" class="rounded-xl border-slate-300" required><button class="rounded-xl bg-emerald-600 px-5 font-bold text-white">Kirim</button></div></form><div class="grid gap-4 md:grid-cols-2">@foreach($items as $item)<article class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><small class="text-slate-500">{{ $item->user->name }}</small><h2 class="font-extrabold">{{ $item->judul }}</h2><p class="text-sm text-slate-600">{{ $item->isi }}</p><form method="POST" action="{{ route('usulan.vote',$item) }}">@csrf<button class="rounded-xl bg-blue-50 px-4 py-2 text-sm font-bold text-blue-700"><i class="fa-solid fa-thumbs-up"></i> Dukung · {{ $item->jumlah_vote }}</button></form></article>@endforeach</div>{{ $items->links() }}</x-app-layout>
