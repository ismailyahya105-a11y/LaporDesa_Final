<x-app-layout><x-slot name="header"><div class="d-flex flex-column flex-md-row justify-content-between align-items-md-end gap-3"><div><span class="eyebrow">Dashboard Masyarakat</span><h1 class="page-title">Selamat datang, {{ explode(' ',auth()->user()->name)[0] }}!</h1><p class="text-muted mb-0">Pantau seluruh laporan dan perkembangannya di sini.</p></div><a href="{{ route('laporan.create') }}" class="btn btn-village"><i class="fa-solid fa-plus me-2"></i>Buat Laporan Baru</a></div></x-slot>
<div class="row g-3 g-xl-4 fade-up">@foreach([['Total Laporan Saya',$total,'fa-file-lines','stat-total'],['Menunggu',$menunggu,'fa-clock','stat-waiting'],['Diproses',$diproses,'fa-arrows-rotate','stat-process'],['Selesai',$selesai,'fa-circle-check','stat-done']] as [$label,$value,$icon,$class])<div class="col-6 col-xl-3"><div class="card smart-stat {{ $class }} border-0 h-100"><div class="card-body"><span class="smart-stat-icon"><i class="fa-solid {{ $icon }}"></i></span><div><small>{{ $label }}</small><strong>{{ $value }}</strong></div></div></div></div>@endforeach</div>
<div class="card content-card mt-4 fade-up"><div class="card-header"><div><h2>Laporan Terbaru</h2><p>Preview laporan yang telah Anda ajukan</p></div><a href="{{ route('laporan.index') }}">Lihat semua <i class="fa-solid fa-arrow-right"></i></a></div><div class="table-responsive"><table class="table align-middle modern-table mb-0"><thead><tr><th>Preview</th><th>Laporan</th><th>Kategori</th><th>Tanggal</th><th>Status</th><th></th></tr></thead><tbody>@forelse($recentReports as $report)<tr><td>@if($report->hasPhotoFile())<img src="{{ route('laporan.photo', $report) }}" alt="Foto {{ $report->judul }}" class="h-14 w-20 rounded-xl object-cover">@else<span class="grid h-14 w-20 place-items-center rounded-xl bg-emerald-50 text-emerald-500"><i class="fa-regular fa-image"></i></span>@endif</td><td><div><strong class="d-block">{{ $report->judul }}</strong><small class="text-muted">{{ \Illuminate\Support\Str::limit($report->isi_laporan, 70) }}</small></div></td><td>{{ $report->kategori->nama }}</td><td class="text-muted text-nowrap">{{ $report->created_at->format('d M Y, H:i') }}</td><td><x-status-badge :status="$report->status" /></td><td><a class="round-action" href="{{ route('laporan.show',$report) }}" aria-label="Lihat detail {{ $report->judul }}"><i class="fa-solid fa-chevron-right"></i></a></td></tr>@empty<tr><td colspan="6"><div class="empty-state"><i class="fa-regular fa-folder-open"></i><h3>Belum ada laporan</h3><p>Mulai sampaikan permasalahan desa di sekitar Anda.</p><a class="btn btn-village btn-sm" href="{{ route('laporan.create') }}">Buat laporan</a></div></td></tr>@endforelse</tbody></table></div></div>
<div class="mt-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="mb-1 text-base font-extrabold">Status Laporan Saya</h2><p class="text-xs text-slate-500">Distribusi progres pengaduan Anda</p><div class="mx-auto h-72 max-w-xl"><canvas id="myStatusChart"></canvas></div></div>
@push('scripts')<script>new Chart(document.getElementById('myStatusChart'),{type:'doughnut',data:{labels:['Menunggu','Diproses','Selesai'],datasets:[{data:@json([$menunggu,$diproses,$selesai]),backgroundColor:['#f59e0b','#2563eb','#16a34a'],borderWidth:0}]},options:{maintainAspectRatio:false,plugins:{legend:{position:'bottom'}}}});</script>@endpush
</x-app-layout>
